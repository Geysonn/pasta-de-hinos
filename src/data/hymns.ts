// Pastas ainda não cadastradas — ficam vazias até você enviar o PDF/fotos dos hinos do grupo.
import type { Hymn } from '../types'

export const demoHarpa: Hymn[] = []

export const demoGeracaoLife: Hymn[] = []
